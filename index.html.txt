<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Giặt Xả Bình Minh - Mang Nắng Mai Vào Từng Sợi Vải</title>
    
    <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js"></script>
    <script type="text/javascript">
        (function() {
            emailjs.init("aAkmS6UReOqL7ucDTiK");
        })();
    </script>
    
    <style>
        :root {
            --primary-color: #0288d1;
            --accent-color: #ffb300;
            --text-color: #333;
            --bg-light: #f4f9fc;
            --success-color: #4caf50;
            --danger-color: #f44336;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            color: var(--text-color);
            background-color: #fff;
            line-height: 1.6;
        }

        header {
            background-color: white;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 20px;
        }

        .logo-area {
            display: flex;
            align-items: center;
            gap: 10px;
            cursor: pointer;
        }

        .logo-img {
            height: 50px;
            width: auto;
            border-radius: 50%;
        }

        .brand-name {
            font-size: 20px;
            font-weight: bold;
            color: var(--primary-color);
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 20px;
        }

        nav ul li a {
            text-decoration: none;
            color: #555;
            font-weight: 600;
            cursor: pointer;
            transition: color 0.3s;
            padding: 5px 10px;
        }

        nav ul li a:hover, nav ul li a.active {
            color: var(--primary-color);
        }

        .cart-icon-btn {
            position: relative;
            background: none;
            border: none;
            cursor: pointer;
            font-size: 22px;
        }

        .cart-count {
            position: absolute;
            top: -8px;
            right: -10px;
            background-color: var(--accent-color);
            color: #fff;
            font-size: 12px;
            font-weight: bold;
            padding: 2px 6px;
            border-radius: 50%;
        }

        .page-section {
            display: none;
            max-width: 1200px;
            margin: 30px auto;
            padding: 0 20px;
        }

        .page-section.active-page {
            display: block;
        }

        .hero-banner {
            background: linear-gradient(135deg, #e0f2fe 0%, #bae6fd 100%);
            padding: 60px 20px;
            text-align: center;
            border-radius: 12px;
            margin-bottom: 40px;
        }

        .hero-banner h1 {
            color: var(--primary-color);
            font-size: 32px;
            margin-bottom: 10px;
        }

        .hero-banner p {
            font-size: 18px;
            font-style: italic;
            color: #555;
        }

        .section-title {
            font-size: 24px;
            color: var(--primary-color);
            margin-bottom: 20px;
            border-left: 5px solid var(--primary-color);
            padding-left: 10px;
        }

        .production-box {
            background: var(--bg-light);
            padding: 20px;
            border-radius: 8px;
            text-align: center;
            margin-bottom: 40px;
        }

        .production-img {
            max-width: 100%;
            height: auto;
            border-radius: 8px;
            margin-top: 15px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.05);
        }

        .featured-products {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .product-card {
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            padding: 15px;
            text-align: center;
            background: #fff;
            box-shadow: 0 2px 5px rgba(0,0,0,0.02);
            cursor: pointer;
            transition: transform 0.2s;
        }

        .product-card:hover {
            transform: translateY(-5px);
        }

        .product-card img {
            max-width: 100%;
            height: 180px;
            object-fit: contain;
            margin-bottom: 10px;
        }

        .commitment-box {
            background-color: #fffde7;
            border: 1px dashed #fbc02d;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 40px;
        }

        .product-container {
            display: grid;
            grid-template-columns: 100%;
        }
        @media (min-width: 768px) {
            .product-container {
                grid-template-columns: 1fr 1fr;
                gap: 40px;
            }
        }

        .image-zoom-area {
            position: relative;
            overflow: hidden;
            border: 1px solid #ddd;
            border-radius: 12px;
            cursor: zoom-in;
            background-color: #fff;
            height: 400px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .image-zoom-area img {
            max-width: 100%;
            max-height: 100%;
            transition: transform 0.3s ease;
        }

        .image-zoom-area:hover img {
            transform: scale(1.6);
        }

        .ingredients {
            background: #f8fafc;
            padding: 15px;
            border-radius: 8px;
            margin: 15px 0;
            font-size: 14px;
            border-left: 3px solid #0288d1;
        }

        .size-selector-frame {
            background: #f1f5f9;
            padding: 15px;
            border-radius: 8px;
            margin: 15px 0;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 15px;
        }

        .size-select-box {
            flex: 1;
            padding: 10px;
            font-size: 16px;
            border-radius: 6px;
            border: 1px solid #cbd5e1;
            background: white;
        }

        .price-display {
            font-size: 24px;
            color: #d32f2f;
            font-weight: bold;
            white-space: nowrap;
        }

        .quantity-control {
            display: flex;
            align-items: center;
            gap: 15px;
            margin: 20px 0;
        }

        .qty-btn {
            width: 35px;
            height: 35px;
            background-color: #e2e8f0;
            border: none;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            border-radius: 4px;
        }

        .qty-value {
            font-size: 18px;
            font-weight: bold;
            width: 30px;
            text-align: center;
        }

        .action-area {
            display: flex;
            align-items: center;
            flex-wrap: wrap;
            gap: 20px;
        }

        .add-to-cart-btn {
            background-color: var(--accent-color);
            color: #333;
            border: none;
            padding: 12px 25px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 6px;
            cursor: pointer;
            transition: background 0.2s;
        }

        .add-to-cart-btn:hover {
            background-color: #f5a000;
        }

        .total-preview-box {
            font-size: 16px;
            font-weight: bold;
        }

        .review-section {
            margin-top: 40px;
            border-top: 1px solid #e2e8f0;
            padding-top: 20px;
        }

        .review-item {
            border-bottom: 1px solid #f1f5f9;
            padding: 15px 0;
        }

        .review-stars {
            color: #ffb300;
        }

        .add-review-box {
            background: #f8fafc;
            padding: 20px;
            border-radius: 8px;
            margin-top: 20px;
        }

        .star-rating-input {
            display: flex;
            gap: 8px;
            margin: 10px 0;
            font-size: 26px;
            color: #cbd5e1;
            cursor: pointer;
        }

        .star-rating-input .star.selected {
            color: var(--accent-color);
        }

        .review-textarea {
            width: 100%;
            height: 80px;
            padding: 10px;
            border-radius: 6px;
            border: 1px solid #cbd5e1;
            resize: none;
            margin-bottom: 10px;
        }

        .story-box {
            background-color: #fff;
            padding: 20px;
            line-height: 1.8;
            font-size: 17px;
            margin-bottom: 30px;
        }

        .factory-box {
            text-align: center;
            background: var(--bg-light);
            padding: 20px;
            border-radius: 12px;
        }

        .factory-img {
            max-width: 100%;
            height: auto;
            border-radius: 8px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.06);
        }

        .cart-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        .cart-table th, .cart-table td {
            border: 1px solid #e2e8f0;
            padding: 12px;
            text-align: left;
        }

        .cart-table th {
            background-color: var(--bg-light);
        }

        .cart-actions-container {
            display: flex;
            gap: 12px;
        }

        .btn-update { background-color: var(--success-color); color: white; border: none; padding: 6px 12px; border-radius: 4px; cursor: pointer; }
        .btn-delete { background-color: var(--danger-color); color: white; border: none; padding: 6px 12px; border-radius: 4px; cursor: pointer; }

        .cart-summary-area {
            margin-top: 30px;
            text-align: right;
            background: var(--bg-light);
            padding: 20px;
            border-radius: 8px;
        }

        .btn-go-checkout {
            background-color: var(--primary-color);
            color: white;
            border: none;
            padding: 12px 30px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 6px;
            cursor: pointer;
            margin-top: 15px;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-weight: 600;
        }

        .form-control {
            width: 100%;
            padding: 10px;
            border: 1px solid #cbd5e1;
            border-radius: 4px;
            font-size: 15px;
        }

        .checkout-layout {
            display: grid;
            grid-template-columns: 100%;
        }
        @media (min-width: 768px) {
            .checkout-layout {
                grid-template-columns: 1.2fr 0.8fr;
                gap: 30px;
            }
        }

        .order-summary-box {
            background-color: var(--bg-light);
            padding: 20px;
            border-radius: 8px;
            border: 1px solid #e2e8f0;
        }

        .payment-option {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 10px;
            background: #fff;
            padding: 12px;
            border: 1px solid #e2e8f0;
            border-radius: 4px;
            cursor: pointer;
        }

        .btn-submit-form {
            width: 100%;
            background-color: var(--primary-color);
            color: white;
            border: none;
            padding: 14px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 6px;
            cursor: pointer;
            margin-top: 15px;
        }

        .alert-box {
            padding: 12px;
            margin: 15px 0;
            border-radius: 6px;
            font-weight: bold;
            display: none;
        }
        .alert-success { background-color: #d4edda; color: #155724; border: 1px solid #c3e6cb; }
        .alert-error { background-color: #f8d7da; color: #721c24; border: 1px solid #f5c6cb; }

        footer {
            background-color: #1e293b;
            color: #94a3b8;
            padding: 30px 20px;
            margin-top: 60px;
            font-size: 14px;
        }

        .footer-container {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 10px;
            text-align: center;
        }

        .footer-info-highlight {
            color: #fff;
            font-weight: 600;
        }
    </style>
</head>
<body>

    <header>
        <div class="nav-container">
            <div class="logo-area" onclick="switchPage('home')">
                <img src="logo.png" alt="Logo Giặt xả Bình Minh" class="logo-